<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/settings"
            android:textColor="@color/white"
            android:textSize="15sp"
            android:textStyle="bold"
            android:gravity="center"
            android:paddingBottom="4dp" />

        <!-- Cookie -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Cookie"
            android:textColor="@color/gray_text"
            android:textSize="11sp"
            android:paddingBottom="2dp" />

        <EditText
            android:id="@+id/et_cookie"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@color/gray_light"
            android:gravity="top|start"
            android:hint="@string/cookie_hint"
            android:inputType="textMultiLine"
            android:padding="6dp"
            android:textColor="@color/black"
            android:textColorHint="@color/gray_text"
            android:textSize="10sp" />

        <!-- Save button -->
        <TextView
            android:id="@+id/btn_save_cookie"
            android:layout_width="match_parent"
            android:layout_height="34dp"
            android:layout_marginTop="4dp"
            android:background="@color/colorPrimary"
            android:gravity="center"
            android:text="@string/save_cookie"
            android:textColor="@color/white"
            android:textSize="13sp"
            android:clickable="true"
            android:focusable="true" />

        <!-- QR Login button -->
        <TextView
            android:id="@+id/btn_qr_login"
            android:layout_width="match_parent"
            android:layout_height="34dp"
            android:layout_marginTop="4dp"
            android:background="@color/colorAccent"
            android:gravity="center"
            android:text="@string/qr_login"
            android:textColor="@color/white"
            android:textSize="13sp"
            android:clickable="true"
            android:focusable="true" />

        <!-- SMS Login button -->
        <TextView
            android:id="@+id/btn_sms_login"
            android:layout_width="match_parent"
            android:layout_height="34dp"
            android:layout_marginTop="4dp"
            android:background="@color/colorPrimary"
            android:gravity="center"
            android:text="@string/sms_login"
            android:textColor="@color/white"
            android:textSize="13sp"
            android:clickable="true"
            android:focusable="true" />

        <!-- Keep screen on toggle -->
        <TextView
            android:id="@+id/btn_keep_screen_on"
            android:layout_width="match_parent"
            android:layout_height="34dp"
            android:layout_marginTop="4dp"
            android:background="@color/bg_dark"
            android:gravity="center"
            android:text="@string/keep_screen_on_off"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:clickable="true"
            android:focusable="true" />

        <!-- Favorites mode toggle -->
        <TextView
            android:id="@+id/btn_fav_mode"
            android:layout_width="match_parent"
            android:layout_height="34dp"
            android:layout_marginTop="4dp"
            android:background="@color/bg_dark"
            android:gravity="center"
            android:text="@string/fav_mode_local"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:clickable="true"
            android:focusable="true" />

    </LinearLayout>
</ScrollView>
